{
  "dependencies": {
    "newsapi": "^2.4.1",
    "sweetalert": "^2.1.2"
  },
  "scripts": {
    "start": "json-server src/database/lancamentos.json --port 3002 && json-server --watch src/database/carteiras.json --port 3001 && json-server --watch src/database/usuarios.json --port 3000"
  }
}
