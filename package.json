{
  "dependencies": {
    "newsapi": "^2.4.1",
    "sweetalert": "^2.1.2"
  },
  "scripts": {
    "start" : "json-server --watch database/usuarios.json --port 3000 &&  json-server --watch carteiras.json --port 3001 && json-server --watch lancamentos.json --port 3002"
 }
}
