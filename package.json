{
  "dependencies": {
    "json-server": "^0.16.3",
    "newsapi": "^2.4.1",
    "sweetalert": "^2.1.2"
  },
  "scripts": {
    "start": "cd src/database && json-server database.json --port 3000"
  }
}
